import React from 'react'

const UserContext =React.createContext()

export default UserContext

{/* <UserContext>
    <Login/>
    //and all the other components, are enclosed in the <UserContext> and may use its value. It is somewhat like a global variable.
<UserContext/> */}